# Production deployment workflow
# Triggers on pushes to 'production' branch for optimized live deployment

name: Deploy to Firebase Hosting Production
on:
  push:
    branches:
      - production
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - name: Deploy to Firebase Hosting (Production)
        run: |
          echo "${{ secrets.FIREBASE_SERVICE_ACCOUNT_EDGEVIDEO_AI_8753A }}" > firebase-service-account.json
          export GOOGLE_APPLICATION_CREDENTIALS=firebase-service-account.json
          npx firebase-tools deploy --only hosting:edgevideo-production --project edgevideo-ai-8753a
